/**
 * my great project
 *
 * @remarks
 * provide {@link TheFormat} function that returns `the-format` string
 *
 * @packageDocumentation
 */

/**
 * @public
 */
export declare interface Options {}

/**
 *
 * return `the-format` string
 *
 * @returns the `the-format`  string
 *
 * @example
 * Here's an example:
 *
 * ```ts
 * import { TheFormat } from 'the-format'
 *
 * console.log(TheFormat());
 * // Prints "the-format":
 * ```
 */
export declare function TheFormat(options?: Options): string;

export {};
