/**
 * @public
 */
export interface Options {}

/**
 *
 * return `the-format` string
 *
 * @returns the `the-format`  string
 *
 * @example
 * Here's an example:
 *
 * ```ts
 * import { TheFormat } from 'the-format'
 *
 * console.log(TheFormat());
 * // Prints "the-format":
 * ```
 */
export function TheFormat(options?: Options) {
  return 'the-format';
}
